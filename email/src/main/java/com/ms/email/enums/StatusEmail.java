package com.ms.email.enums;

/**
 * This enum represents the status of an email.
 * It has two possible values:
 * SENT: Indicates that the email was sent successfully.
 * ERROR: Indicates that there was an error while sending the email.
 */
public enum StatusEmail {
    SENT,
    ERROR;
}
